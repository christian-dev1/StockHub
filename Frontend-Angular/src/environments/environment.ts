import { AppEnvironment } from '../app/core/config/environment/app-environment';

export const environment: AppEnvironment = {
  production: true,
  apiBaseUrl: '/api/v1',
  actuatorBaseUrl: '/actuator',
  defaultLanguage: 'fr',
};
