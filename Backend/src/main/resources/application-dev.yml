spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5440/stockhub}
    username: ${DB_USERNAME:stockhub}
    password: ${DB_PASSWORD:stockhub}

logging:
  level:
    com.stockhub: DEBUG
    org.hibernate.SQL: INFO

stockhub:
  auth:
    # Development-only key; production requires JWT_SECRET.
    jwt-secret: ${JWT_SECRET:ZGV2LW9ubHktc3RvY2todWItc2VjcmV0LWtleS1kby1ub3QtdXNlLWluLXByb2R1Y3Rpb24tMDEyMzQ1Njc4OQ==}
  bootstrap:
    super-admin:
      email: ${STOCKHUB_SUPERADMIN_EMAIL:superadmin@stockhub.local}
      password: ${STOCKHUB_SUPERADMIN_PASSWORD:SuperAdmin2026}
  cors:
    allowed-origins: ${STOCKHUB_CORS_ALLOWED_ORIGINS:http://localhost:4300,http://localhost:3100}
