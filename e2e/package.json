{
  "name": "stockhub-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end tests run against the full Docker stack",
  "scripts": {
    "test": "playwright test --project=e2e",
    "smoke": "playwright test --project=smoke",
    "test:all": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0",
    "@types/node": "^22.20.4",
    "typescript": "^5.9.3"
  }
}
